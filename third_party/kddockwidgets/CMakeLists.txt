set(KDDockWidgets_QT6 ON)
set(KDDockWidgets_EXAMPLES OFF)
set(KDDockWidgets_QTQUICK ON)
if (BUILD_SHARED_LIBS)
    set(KDDockWidgets_STATIC OFF)
else()
    set(KDDockWidgets_STATIC ON)
endif()
add_subdirectory(kddockwidgets)
